<!DOCTYPE html>
<html lang="es">
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <title><%= titulo %></title>
        <link rel="stylesheet" href="/css/style.css">
    </head>
    <body>
        <div class="container">
            <h1><%= titulo %></h1> 
               <form action="/editar/<%= contacto.id %>" method="POST" class="contact-form">
        <!-- El ID se mantiene oculto para la actualización -->
                    <input type="hidden" name="id" value="<%= contacto.id %>"> 

                    <label for="nombre">Nombre:</label>
                     <input type="text" id="nombre" name="nombre" value="<%= contacto.nombre %>" required>

                    <label for="telefono">Teléfono:</label>
                        <input type="tel" id="telefono" name="telefono" value="<%= contacto.telefono %>" required>

                    <label for="email">Email:</label>
                        <input type="email" id="email" name="email" value="<%= contacto.email %>" required>
        
                    <button type="submit" class="button submit-button update-button">Actualizar Contacto</button>
                </form>

            <p><a href="/" class="back-link">← Volver a la Lista</a></p>
        </div>
    </body>
</html>